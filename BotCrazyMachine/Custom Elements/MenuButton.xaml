<Button x:Class="BotCrazyMachine.Custom_Elements.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            MouseEnter="Button_MouseEnter"
            MouseLeave="Button_MouseLeave"
            Style="{StaticResource CustomMenuButton}"
             xmlns:local="clr-namespace:BotCrazyMachine.Custom_Elements"
             mc:Ignorable="d">
 
    <Grid Margin="0"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="12*"/>
        </Grid.ColumnDefinitions>
        <Border x:Name="MenuButtonSidebar" Background ="#F03E2B" Visibility="Hidden" Grid.Column="0" CornerRadius="5" Width="5" HorizontalAlignment="Left" VerticalAlignment="Stretch"/>
        <StackPanel  Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Image x:Name="MenuButtonIcon"  HorizontalAlignment="Stretch" Height="30" Width="25"/>
            <Label x:Name="MenuButtonText"   HorizontalAlignment="Center" Foreground="White" Padding="0" FontSize="14"/>
        </StackPanel>
    </Grid>
</Button>
